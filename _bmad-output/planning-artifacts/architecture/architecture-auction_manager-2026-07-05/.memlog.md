---
scope: Auction Manager v1 local live-event app
purpose: build-substrate
altitude: feature
updated: 2026-07-06T18:36
---

- (constraint) Source PRD is final and defines local single-PC operation, localhost web app, mirrored display, no accounts, no public deployment, no online bidding, import-only setup, hard-block auction rules, multi-step undo, local state files, and dangerous reset/close operations.
- (constraint) UX inputs bind the main phases: Setup, Initial Auction, Unsold Assignment, Closed; live board must keep current player, current bid, selected team, team state, undo, and dangerous operations legible and separated.
- (version) Verified Node.js release table on 2026-07-05: v24 is LTS, v26 is Current, and production apps should use Active or Maintenance LTS; bind runtime to Node.js 24 LTS.
- (version) Verified current docs/package lines on 2026-07-05: React 19.2, Vite 8.1.2, Fastify latest v5.9.x, Tailwind CSS v4.3, Zod 4, SQLite 3.53.3, better-sqlite3 latest v12.11.2, Sharp 0.35.3, Vitest 4.1.9, Playwright release notes v1.61.
- (decision) AD-1: Use a local modular monolith with one event-mode Node process and logical layers web, server, domain, persistence, imports, and shared contracts; binds all v1 capabilities; prevents cloud/distributed or browser-only implementations from diverging on reliability and local operation.
- (decision) AD-2: packages/domain is the sole authority for auction phases, rules, commands, randomization, random assignment eligibility, and undo semantics; binds FR-5, FR-7, FR-11 through FR-19; prevents duplicated rule logic in React, Fastify routes, and persistence.
- (decision) AD-3: Server/domain state is authoritative; React owns only view state and submitted command intent; every successful mutation returns the authoritative board-ready state DTO; binds live board, undo, resume, privacy; prevents client-side state drift and inconsistent projected display.
- (decision) AD-4: Use command-oriented same-origin HTTP over localhost: GET /api/state for reads and POST commands for setup/live mutations, with Fastify route schemas and clientCommandId on mutating commands; binds all UI/API boundaries; prevents generic CRUD updates, CORS drift, and duplicate command effects.
- (decision) AD-5: Persist authoritative state in SQLite current-state tables plus action_log and latest JSON snapshot; every state-changing command runs in one SQLite transaction and app hard-stops further mutations after persistence failure; binds FR-19, FR-20, reliability; prevents partial sales, lost undo history, and in-memory-only live drift.
- (decision) AD-6: Model undo as domain inverse/before-after payloads in action_log for reveal, select team, increase bid, mark sold, mark unsold, and random assignment only; reset and close are non-undoable dangerous commands; binds FR-19, FR-21, FR-22; prevents inconsistent undo scope.
- (decision) AD-7: Treat setup imports as adapters: CSV rows, photos, team CSV, and logos are parsed into staged import results, normalized/copied to managed assets, and converted to structured import_issues before Start Auction; binds FR-1 through FR-6; prevents live-flow dependence on arbitrary source files or hidden bad data.
- (decision) AD-8: Display and API state use privacy-filtered DTOs that include only auction-relevant fields; private registration fields may be imported only for diagnostics when needed and must never enter board DTOs or live UI models; binds PRD privacy and UX privacy floor; prevents accidental projected exposure.
- (decision) AD-9: Local file security boundary: bind event server to 127.0.0.1 by default, serve API/UI/assets same-origin, allowlist uploads, set size/count limits, strip source filenames/paths, generate internal asset IDs, and never serve arbitrary filesystem paths; binds setup imports and local operation; prevents path traversal, storage bloat, and accidental LAN exposure.
- (decision) AD-10: Event mode is one Fastify process serving built React assets, normalized assets, health, and /api on localhost with a writable data directory check before startup; development may use Vite dev server plus API proxy; binds operations/recovery; prevents Docker/cloud/multi-process requirements during the live event.
- (decision) AD-11: Tests are layered around invariants: domain command tests are mandatory for rule or phase changes, persistence tests cover transaction/resume/action_log/snapshot, Fastify inject tests cover route schemas/conflicts, and Playwright covers setup, live sale, invalid sale, undo, resume, unsold assignment, and privacy; binds quality gates; prevents UI-only validation confidence.
- (decision) AD-12: Implementation order is scaffold -> domain -> persistence -> API -> imports/assets -> operator/display UI -> event-mode rehearsal; each slice must leave the system buildable/testable; binds epic/story sequencing; prevents a polished UI without recoverable auction correctness.
- (assumption) [ASSUMPTION] HEIC handling remains medium risk until real player photos are tested on the target event PC; Sharp is still the setup normalization choice, but implementation must include sample-asset rehearsal before event readiness.
- (direction) Deferred: separate audience-only route, LAN/multi-device display, auth/RBAC, hosted mode, desktop packaging, Docker-required runtime, custom pricing model, budget impact for random assignment, advanced analytics, long-term league management, Google API/Drive integration, GraphQL/gRPC/WebSocket/event-broker architecture.
- (version) Reviewer correction: csv-parse line is 5.5.x per csv-parse changelog; previous 6.x wording belonged to the umbrella CSV package context, not the selected parser package.
- (decision) Reviewer tightening: AD-7 now fixes Team CSV canonical headers as Team Name and Captain Name, with any aliases isolated to import adapters/tests, to prevent setup stories from choosing incompatible source contracts.
- (decision) Reviewer tightening: AD-9 now requires event-mode CORS disabled and mutating routes to reject unexpected Origin, Host, and Content-Type, with multipart limited to setup uploads, to prevent cross-origin localhost command drift.
- (event) spine finalized
- (constraint) Source PRD updated on 2026-07-05: stakeholder inputs now bind editable Auction Parameters during Auction Creation, role-based base prices, operator manual unsold assignment, and configurable manual-assignment budget behavior; architecture update must replace stale fixed base price and random-assignment rules.
- (decision) Update AD-2: packages/domain remains the sole authority for auction phases, rule validation, editable Auction Parameter locking, derived base prices, random initial player order, manual unsold assignment eligibility, configurable assignment budget effects, and undo semantics; binds FR-2, FR-5, FR-7, FR-11 through FR-19; prevents setup, UI, routes, and persistence from hard-coding divergent pricing or assignment rules.
- (decision) Update AD-6: undo is implemented from action-log inverse or before/after payloads for reveal, select team, increase bid, mark sold, mark unsold, and manual unsold assignment; undo restores affected player, team, bid, selected-team, role-count, budget, assignment-budget effect, and phase fields; reset and close remain non-undoable dangerous commands.
- (decision) AD-13: Auction Parameters are captured in setup, persisted with the auction, and locked after Start Auction; they include role base prices, bid increment, team budgets, player cap, role targets, and manual-assignment budget behavior; binds FR-2, FR-5, FR-6, FR-12 through FR-18, FR-20; prevents implementation slices from treating current league defaults as hard-coded application constants or changing rules mid-auction.
- (direction) Deferred update: remove custom player pricing model and budget impact for random assignment as stale deferrals; retain richer assignment optimization only as future work if manual assignment fairness becomes a stakeholder concern.
- (decision) Reviewer tightening: AD-13 now fixes manual-assignment budget behavior as a shared/domain enum with v1 default NoBudgetImpact, and requires any additional enum values to land with domain validation, persistence, API schema, and tests in the same slice; prevents UI, API, and domain stories from inventing incompatible assignment-budget semantics.
- (event) spine finalized after stakeholder input update
- (constraint) Source PRD updated on 2026-07-06: unsold handling is now three-phase flow. Phase 1 unsold players enter a randomized Phase 2 Unsold Bidding order; players still unsold after Phase 2 enter Phase 3 Manual Assignment. FR-16 starts second-phase bidding, FR-17 runs second-phase bidding, and FR-18 runs third-phase manual assignment.
- (decision) AD-14: Three-phase auction state machine separates InitialAuction, UnsoldBidding, and ManualAssignment. Domain owns transitions, phase-entry gates, persisted randomized Phase 2 order, Phase 2 bidding behavior, Phase 2 unsold-to-manual-assignment movement, and Phase 3 assignment eligibility; binds FR-15 through FR-18, FR-19, FR-20, SM-4; prevents stories from collapsing unsold bidding into manual assignment or treating Phase 2 as random assignment.
- (decision) Update conventions for PRD three-phase flow: phase names are Setup, InitialAuction, UnsoldBidding, ManualAssignment, Closed; domain commands include StartUnsoldBidding and StartManualAssignment, while RevealNextPlayer, SelectTeam, IncreaseBid, MarkSold, MarkUnsold, and Undo remain phase-aware bidding commands; AssignManualPlayer handles Phase 3 assignment.
- (decision) Update test invariant: phase-change work must cover Phase 2 randomized unsold bidding order persistence, Phase 2 sale/unsold behavior, Phase 2 undo, transition to ManualAssignment, Phase 3 assignment validity, and restart/resume across both unsold phases.
- (decision) Update AD-6: undo scope includes StartUnsoldBidding and StartManualAssignment because the PRD user journeys require recovery when an unsold phase is started too early; undo must restore phase, current player, pending pools, skipped-player markers if any, and persisted randomized order state as needed.
- (version) Verified stack on 2026-07-06 against project lockfile, local runtime, Node release page, React versions page, Fastify GitHub releases, and npm registry metadata. Scaffold/runtime versions: Node 24.18.0 LTS, npm 11.16.0, TypeScript 6.0.3, React 19.2.7, Vite 8.1.3, Fastify 5.9.0, SQLite 3.53.2 via better-sqlite3, better-sqlite3 12.11.1, Zod 4.4.3, csv-parse 5.6.0, Sharp 0.35.3, Tailwind CSS 4.3.2, Lucide React 1.23.0, Vitest 4.1.9, Playwright 1.61.1. Registry latest differs for Fastify 5.10.0, csv-parse 7.0.1, and Vitest 4.1.10; architecture stack remains aligned to the current scaffold unless dependencies are intentionally refreshed.
- (constraint) Source precedence for this architecture update: the PRD updated on 2026-07-06 supersedes older UX and technical-research references to UnsoldAssignment, random assignment, or immediate unsold manual assignment. Companion UX/research artifacts should be refreshed before downstream story generation, but the spine now binds implementation to the PRD three-phase unsold flow.
- (event) spine finalized after 2026-07-06 PRD three-phase unsold-flow update
- (decision) AD-15: Team rosters are derived read projections from Player ownership, not independently mutable Team state; MarkSold, AssignManualPlayer, Undo, Resume, and Close must return or expose authoritative roster projection data; binds FR-9, FR-14, FR-18, FR-20, FR-22 and UX Team rosters; prevents duplicate roster stores, stale final screens, and UI/API/persistence disagreement over which players a Team owns.
- (constraint) UX update requires Team rosters as a first-class room-facing surface: operators can switch Board/Rosters during live phases, roster rows update after sold/manual assignment/undo/resume, and Close Auction automatically displays final Team rosters.
- (decision) Update AD-8 and shared DTO conventions: roster projections may expose only auction-relevant fields already allowed by the privacy model plus acquisition type; private registration fields remain excluded from board, roster, logs, and snapshots.
- (decision) Update test invariant: roster work must cover MarkSold roster addition, Phase 2 sold roster addition, Manual Assignment roster addition, Undo removal/restoration, resume reconstruction, privacy projection, and Close Auction final-roster display.
- (decision) Reviewer tightening for AD-15: CloseAuction changes phase/final display only and must not change Player ownership; FR-14, FR-18, FR-20, and FR-22 capability mappings now cite AD-15 so roster projection updates are testable where ownership changes or final display occurs.
- (event) spine finalized after Team rosters UX update
